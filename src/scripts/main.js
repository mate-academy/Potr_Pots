'use strict';
// const imageContainer = document.querySelector('.header__img');

// imageContainer.addEventListener('mousemove', (e) => {
//   const { left, top, width, height } = imageContainer.getBoundingClientRect();
//   const x = (e.pageX - left) / width; // Визначити відносну позицію курсора по горизонталі
//   const y = (e.pageY - top) / height; // Визначити відносну позицію курсора по вертикалі

//   imageContainer.style.setProperty('--x', x);
//   imageContainer.style.setProperty('--y', y);
// });
